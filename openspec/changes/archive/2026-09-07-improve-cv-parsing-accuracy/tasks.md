## 1. Header dictionary and segmentation-state changes

- [x] 1.1 In `src/domain/services/section-segmenter.ts`, add a `"summary"` member to `CvSectionKey` and a `summary` entry to `SECTION_HEADER_DICTIONARY` with the EN/ES phrases from `specs/cv-parsing/spec.md` (Header/Summary Extraction). Add `"herramientas"` and `"tools"` to the existing `skills` dictionary entry. Verify with a `section-segmenter.test.ts` case asserting a line under a "Perfil Profesional"/"Summary" header lands in `result.sections.summary`, and a case asserting "Herramientas"/"Tools" content lands in `result.sections.skills`.
- [x] 1.2 Add a second, non-exposed dictionary of achievement/award header phrases (EN/ES, from the spec's Section Segmentation scenario) and change `segmentCv`'s loop state from `currentSection: CvSectionKey | null` to a three-way mode (`personal-info` / a tracked `CvSectionKey` / `"ignored"`), so lines encountered after an achievements-style header are dropped instead of appended to the previously active section or to `personalInfoLines`, until the next recognized header. Verify with a `section-segmenter.test.ts` case: content after "Logros y Distinciones" (following an Education section) does not appear in `sections.education` and does not appear in `personalInfoLines`.
- [x] 1.3 Add `detectLowConfidenceSegmentation(lines, allRecognizedPhrases): string | null` to `section-segmenter.ts` (or a co-located helper), implementing the all-caps-overlap heuristic from `design.md` (Low-confidence detection). Verify with unit tests for all three scenarios in the spec: a concatenated all-caps line (e.g. "CONTACTO EXPERIENCIA PROFESIONAL") triggers a warning; an ordinary prose line mentioning a header word (e.g. "5 years of experience in backend systems") does not; a CV with every header alone on its own line does not.
- [x] 1.4 Add `warning: string | null` to the `SegmentedCv` interface, populated by calling the function from 1.3 inside `segmentCv`. Verify via a `section-segmenter.test.ts` case asserting `segmentCv(...).warning` is set for the concatenated-header case and `null` otherwise.

## 2. Response contract

- [x] 2.1 Add `summary: string | null` to `ParsedCvResult` in `src/domain/value-objects/parsed-cv-result.ts`, defaulting to `null` when omitted in `createParsedCvResult`. Verify via a `value-objects.test.ts` case asserting the default is `null` and a provided value is preserved.
- [x] 2.2 In `src/application/use-cases/parse-cv-use-case.ts`, derive `summary` from `segmented.sections.summary` (join non-empty lines with `"\n"`, trim, `null` if empty) and pass it into `createParsedCvResult`. Verify via a `parse-cv-use-case.test.ts` case with a "Summary" header block in the input lines, asserting `result.summary` equals the expected joined text and none of that text leaks into `result.personalInfo`.
- [x] 2.3 In the same use case, append `segmented.warning` (from task 1.4) to the assembled `warnings` array when non-null, alongside the existing experience/education split warnings. Verify via a `parse-cv-use-case.test.ts` case using a concatenated-all-caps-header input, asserting `result.warnings` contains the low-confidence segmentation warning.

## 3. UI

- [x] 3.1 Create `src/components/cv/summary-section.tsx` (mirroring `skills-section.tsx`'s structure: `SectionCard` wrapper, an unused accent color e.g. `bg-teal-500`, empty message "No summary was found.") rendering `result.summary` as a paragraph.
- [x] 3.2 Wire `SummarySection` into `src/components/cv/cv-result-view.tsx`: add `"summary"` to the `SectionId` union, render it between Personal Info and Experience, keep `FIRST_SECTION_ID` as `"personal-info"`.
- [x] 3.3 In `cv-result-view.tsx`, render any warning not claimed by an existing `findWarningFor` keyword (i.e. the document-wide low-confidence segmentation warning) as a general, non-destructive `Alert` notice above the section cards, distinct from `ErrorBanner`. Verified end-to-end against the real dev server (`npm run dev` + a hand-built PDF containing "CONTACTO EXPERIENCIA PROFESIONAL") via a direct POST to `/api/parse-cv`: the response's `summary` field and the low-confidence warning both come back correctly (see session output). No browser/screenshot tool was available in this environment to visually confirm the rendered `Alert`/`SummarySection` DOM output, so that specific visual check is unverified — `tsc --noEmit` passes for the changed components, and the data they render was confirmed correct at the API boundary.

## 4. Regression tests from real-CV structural shapes (fabricated data only)

- [x] 4.1 Add `section-segmenter.test.ts` / `personal-info-extractor.test.ts` cases modeled on a multi-employer Spanish CV with "Herramientas" (Skills) and "Logros y Distinciones" (achievements) headers, using an invented name, email, phone, and genericized (non-verbatim) employer/institution names. Verify skills are captured, achievement content is excluded from Education, and personal info is not polluted.
- [x] 4.2 Add `section-segmenter.test.ts` / `parse-cv-use-case.test.ts` cases modeled on an English CV with a multi-sentence "Summary" paragraph that includes a forward-looking statement (e.g. "Looking to contribute in a senior ... role"), using an invented name/email/phone. Verify `summary` captures that text and none of it appears in `personalInfoLines` or the extracted `personalInfo` fields.
- [x] 4.3 Add a `section-segmenter.test.ts` case modeled on a sidebar-style CV reproducing an all-caps concatenated header line (e.g. "CONTACTO EXPERIENCIA PROFESIONAL"), using an invented name. Verify `detectLowConfidenceSegmentation`/`segmentCv(...).warning` fires and the line's content still lands in the active section rather than creating a spurious new one.
- [x] 4.4 Confirm no new files were added under `src/infrastructure/pdf/__fixtures__/`, no test reads a file from outside the repo (e.g. `~/Downloads`), and no test file or fixture contains the three real CVs' actual names, emails, phone numbers, or verbatim employer/institution names — grep the diff for any such leakage before committing.

## 5. Verification

- [x] 5.1 Run `npm run lint` and `npm test` and confirm all tests pass, including every new case added in tasks 1-4.
- [x] 5.2 Re-read `openspec/specs/cv-parsing/spec.md` and `openspec/specs/cv-results-display/spec.md` deltas against the final implementation and confirm every new/modified scenario is covered by at least one automated test or the manual UI check from 3.3. Audit result: every cv-parsing scenario has a direct automated test. Every cv-results-display scenario's underlying logic is either unchanged (existing SectionCard collapse behavior) or automated (warning-partitioning was extracted to `warning-grouping.ts` specifically so it has real test coverage instead of living untested inline in a component); the remaining gap is that no tool in this environment could render the page in a browser to visually confirm DOM/CSS output — verified instead via a real end-to-end API call (3.3) plus `tsc --noEmit`.
